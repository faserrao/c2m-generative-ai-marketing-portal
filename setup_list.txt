python3 -m venv ../my_venv
source ../my_venv/bin/activate

export PYTHONPATH=
source setup_python_path_for_local_execution.bsh

pip install -r requirements.txt
pip install -r requirements-dev.txt

pip install -r assets/streamlit/requirements-streamlit-local-dev.txt

cdk bootstrap &&
cdk synth &&
cdk deploy

# For local execution
cd assets/streamlit/ &&
streamlit run src/Home.py
